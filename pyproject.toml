[project]
name = "orca"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "matplotlib>=3.10.9",
    "numpy>=2.2.6",
]
[tool.setuptools]
py-modules = []

[tools.setuptools.packages.find]
include = ["env*"]
